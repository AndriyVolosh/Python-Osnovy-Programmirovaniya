Принцип перевода кода с Python на PHP - по одному усложнению с каждой новой задачей.

1. Hello,%username! "Hello,_username!.php"
  (!В php в имени файла недопустимо наличие пробелов!)
  (<? php )
2. Penguins (!В php в имени файла недопустимо наличие русских букв!)
  (# считываем в строковую переменную значение из ввода -> //получаем значение из _POST -> 
  //проверяем существование post-переменной функцией filter_has_var())
3. Дележ яблок-1 "Share_of_apples-1.php"
  (в HTML задаем форму <form>)
  //проверяем post-переменную функцией filter_input()
4. Дележ яблок-2  "Share_of_apples-2.php"
  (в HTML задаем поле ввода <input>)
  //проверяем post-переменную на пустоту strlen('username') > 0
5. Степень двойки (в HTML задаем кнопку отправки данных формы на сервер <input type="submit">)
  //записываем post-переменную в переменную скрипта $username = filter_input(INPUT_POST, 'username');
6. Последняя цифра (в HTML задаем название страницы <title>)
  //для умножения строк используем str_repeat()
7. Первая цифра двузначного числа (в HTML задаем названия полей ввода <label>)
  //проверяем post-переменную на наличие числа filter_input(INPUT_POST, 'N', FILTER_VALIDATE_INT);
8. Сумма цифр трехзначного числа (в HTML записываем блок РНР с помощью альтернативного синтаксиса управляющих структур)
  //меняем пробелы в рисунке пингвина на &nbsp;
9. 100A (Подлючаем Bootstrap)
  //Добавляем комментарии к PHP-коду
10. Электронные часы-1 (!В php в имени файла недопустимо наличие знака "%")
  //Целочисленное деление - функция intdiv(php7) или floor(php5)
11. Стоимость покупки (в HTML включаем контейнеры Bootstrap: container, mainbox, panel)
