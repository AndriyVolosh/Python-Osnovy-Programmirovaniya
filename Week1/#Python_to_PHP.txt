Принцип перевода кода с Python на PHP - по одному усложнению с каждой новой задачей.

1. Hello,%username! (!В php в имени файла недопустимо наличие пробелов!)
  (<? php )
2. Penguins (!В php в имени файла недопустимо наличие русских букв!)
  (# считываем в строковую переменную значение из ввода -> //получаем значение из _POST -> 
  //проверяем существование переменной функцией filter_has_var())
3. Дележ яблок-1 (в HTML задаем форму <form>)
  //проверяем переменную функцией filter_input()
4. Дележ яблок-2 (в HTML задаем поле ввода <input>)
  //проверяем переменную на пустоту strlen() > 0
